export default function HamburgerMenu({ onClick, isOnTouch }) {
    return (
        <footer className="footer">
            <hr className="hr-footer-line" />
            <p
                className="text-gray">
                Copyright.
            </p>
        </footer>           
    );
}